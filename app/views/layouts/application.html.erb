<!DOCTYPE html>
<html>
  <head>
    <title>GeekApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
   <header class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
      <h1 class="my-0 mr-md-auto font-weight-normal"><%= link_to "Instrumovie", contents_path %></h1>
      <div class="my-2 my-md-0 mr-md-3">
      <ul>
      <li class="d-flex">
         <%= form_tag({controller:"contents",action:"search"}, method: :get) do %>
            <%= text_field_tag :search %>
            <%= submit_tag '検索する' , class:"btn btn-primary" %>
          <% end %>
      </li>
        <li class="p-2 text-dark">
            <% if user_signed_in? %>
              <%= link_to "マイページへ", user_path(current_user.id) %>
            <% else %>
              <%= link_to "ログイン", new_user_session_path, class:"btn btn-primary" %>
            <% end %>
        </li>
        <li class="btn btn-outline-primary"><%= link_to "新規投稿", new_content_path %></li>
      </ul>
      </div>
    </header>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
